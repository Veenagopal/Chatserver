services:
  - type: web
    name: Chatserver
    env: python              # ✅ use env instead of runtime
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    pythonVersion: 3.11.9    # ✅ move outside build section
